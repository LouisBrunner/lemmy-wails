name: Build targets
description: Build all targets for the current host
runs:
  using: composite
  steps:
    - name: Setup
      uses: ./.github/actions/setup

    - name: Build
      shell: bash
      run: make build-all
